#include <stdio.h>

#define count 50

typedef struct {
    char n[50], fn[50], ln[50];
    int year;
} humen;

int main() {
    humen a[count], b[count];
    int i, j;
    humen temp;




    return 0;
}